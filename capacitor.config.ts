import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danmolna.qrassetscanner',
  appName: 'QR Asset Scanner',
  webDir: 'www'
};

export default config;
